name: ${now:%y%m%d-%H%M%S}_${slug:2}

tags: []
description: null

pre_fit_validation: true
fit: true
test: true
predict: false

seed: null

# Resume training from checkpoint
resume:
  ckpt_path: null  # Path to checkpoint file to resume from (e.g., logs/exp_name/run_name/checkpoints/last.ckpt)
